/** 
*! package.json
 stores applciation- file name,version,version, commands,and package information 
       which we have installed.

*? How to install packages?
 1. npm i react-validation
 2. just copy version of packge in package.json and run command
   *? npm install 

How to uninstall packages?
 1. npm uninstall react-validation
 2. just remove version of packge in package.json and run command
   *? npm uninstall 
   this will detect automatically and delete that file.

*? Create custom commands
  just edit scripts in package.json file and rename as that 
  default command  

*/